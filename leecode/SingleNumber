/**
*只出现一次的数字
*输入: [2,2,1] 
*输出: 1
**/

class SingleNumber {
    public int singleNumber(int[] nums) {
        int n = 0;
        for (int i = 0; i < nums.length; i++) {
            n ^= nums[i];
        }
        return n;
    }
}
